# Falha o `npm install` com mensagem clara em quem estiver abaixo do Node exigido.
# O cliente do Prisma é gerado em TypeScript, e o Node só roda TypeScript sem configuração a partir do 22.18.
engine-strict=true
